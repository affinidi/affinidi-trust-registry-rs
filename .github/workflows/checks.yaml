name: checks

on:
  pull_request:
    types:
      - opened
      - synchronize
jobs:
  rust-pipeline:
    uses: affinidi/pipeline-rust/.github/workflows/checks-no-test.yaml@main
    secrets: inherit
    with:
      toolchain: '1.96.0'
      rustflags: '-D warnings --cfg tracing_unstable'
      auditIgnore: 'RUSTSEC-2022-0040,RUSTSEC-2023-0071,RUSTSEC-2024-0373,RUSTSEC-2026-0098,RUSTSEC-2026-0099,RUSTSEC-2026-0104'
  test:
    name: Test Suite
    runs-on: ubuntu-24.04
    env:
      IN_PIPELINE: 'true'
      MEDIATOR_DID: ${{ vars.MEDIATOR_DID }}
      MEDIATOR_URL: ${{ vars.MEDIATOR_URL }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref }}
      - uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1
        with:
          rustflags: ''
          toolchain: '1.96.0'
          override: true
      - uses: affinidi/pipeline-rust/.github/actions/rust-dependencies@main
      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2
        with:
          shared-key: 'rust-cache'
      - name: generate configs
        run: |
          cargo run --bin setup-trust-registry --features dev-tools -p trust-registry -- \
            --mediator-did="$MEDIATOR_DID" \
            --test-in-pipeline=true \
            --file-storage-path=trust-registry/tests/tmp/data.csv
      - name: Setup environment
        run: cp .env.pipeline .env.test
      - name: Prepare test data
        run: |
          mkdir -p trust-registry/tests/tmp
          cp sample-data/data.csv trust-registry/tests/tmp/data.csv
      - name: Run trust-registry server
        run: |
          cp .env.pipeline .env
          RUST_LOG=info nohup cargo run --bin trust-registry > server.log 2>&1 &
          echo $! > server.pid
      - name: Wait for server to be ready
        run: |
          timeout 240 bash -c 'until curl -f http://127.0.0.1:3233/health 2>/dev/null; do sleep 2; done' || (
            echo "Server failed to start. Logs:"
            cat server.log
            exit 1
          )
      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@ea647c55f74bb153499600c054578c0d83fa19d5 # cargo-llvm-cov
      - name: Generate code coverage
        run: |
          tail -n +1 -f server.log &
          TAIL_PID=$!
          trap 'kill $TAIL_PID 2>/dev/null || true' EXIT
          cargo llvm-cov --all-features -p trust-registry --summary-only --fail-under-lines=45
      - name: Stop server
        if: always()
        run: |
          if [ -f server.pid ]; then
            kill $(cat server.pid) || true
          fi
